# Arduino Discord Logger

## Requirements

-   Node.js
-   ArduinoJson.h (library)
-   LiquidCrystal_I2C.h (library)

## Installation

-   Upload Arduino code.
-   Rename `.env.example` to `.env`.
-   Run `npm install` and `npm start`.
